apiVersion: v1
kind: Service
metadata:
  name: collegia-frontend-service
spec:
  type: LoadBalancer
  selector:
    app: collegia-frontend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80